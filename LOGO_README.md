# Логотип и Фавикон dopomoga.me

## 📁 Структура файлов

```
/dopomoga
├── favicon.svg                    # Фавикон (32×32) - одна бирюзовая рыба
├── logo-full.svg                  # Полный круговой логотип (200×200)
├── logo-horizontal.svg            # Горизонтальный логотип с текстом (300×80)
├── logo-compact.svg               # Компактный вариант (120×40)
└── components/
    └── Logo.tsx                   # React компонент логотипа
```

## 🎨 Варианты использования

### Компонент Logo.tsx

```tsx
import Logo from './components/Logo';

// Иконка (для фавикона, маленьких размеров)
<Logo size={32} variant="icon" />

// Полный круговой логотип
<Logo size={200} variant="full" showText={true} />

// Горизонтальный (для навигации)
<Logo size={80} variant="horizontal" />

// Компактный (для мобильных)
<Logo size={40} variant="compact" />
```

### Параметры компонента

- `size` - размер в пикселях (по умолчанию: 48)
- `variant` - вариант логотипа:
  - `'icon'` - одна бирюзовая рыба (для фавикона)
  - `'full'` - круговой логотип с рыбами
  - `'horizontal'` - горизонтальный с текстом
  - `'compact'` - компактный вариант
- `showText` - показывать ли текст (только для `full`)
- `className` - дополнительные CSS классы

## 🎨 Цветовая схема

| Элемент | Цвет | HEX |
|---------|------|-----|
| Бирюзовая рыба (главная) | Яркий бирюзовый | `#00D4AA` |
| Серые рыбы | Приглушённый серо-синий | `#6B7B8A` |
| Фон круга | Тёмно-синий | `#1E3A5F` |
| Текст (светлый фон) | Тёмный графит | `#1A1A2E` |
| Текст (тёмный фон) | Кремовый белый | `#F8F6F2` |

## 📐 Размеры для экспорта

### Фавикон
- `favicon.svg` - 32×32 (уже создан)
- `favicon.ico` - 16×16, 32×32, 48×48 (нужно сгенерировать)
- `apple-touch-icon.png` - 180×180
- `android-chrome-192x192.png` - 192×192
- `android-chrome-512x512.png` - 512×512

### Логотип
- `logo-full-color.png` - 1000×1000 (для печати, презентаций)
- `logo-white.svg` - для тёмного фона
- `logo-dark.svg` - для светлого фона
- `og-image.png` - 1200×630 (для соцсетей)

## 🔧 Генерация дополнительных форматов

Для генерации PNG и ICO файлов из SVG можно использовать:

1. **Online инструменты:**
   - [CloudConvert](https://cloudconvert.com/svg-to-png)
   - [Convertio](https://convertio.co/svg-png/)

2. **Command line (ImageMagick):**
   ```bash
   # PNG из SVG
   convert logo-full.svg -resize 1000x1000 logo-full-color.png
   
   # ICO из SVG
   convert favicon.svg -resize 32x32 favicon.ico
   ```

3. **Node.js (sharp):**
   ```javascript
   const sharp = require('sharp');
   await sharp('logo-full.svg').resize(1000, 1000).png().toFile('logo-full-color.png');
   ```

## 📝 Концепция

Логотип основан на символике **"рыбы против течения"** — визуальная метафора метанойи (разворота жизни на 180°).

- **Серые рыбы** (8-10 штук) - плывут по часовой стрелке, символизируют "жизнь по течению"
- **Бирюзовая рыба** (1 штука) - плывёт против течения, выделяется цветом и свечением (glow)
- **Круг** - символ единства, цикличности, общности

## 🚀 Использование в проекте

Логотип уже интегрирован в компонент `Hero.tsx`:

```tsx
<Logo size={32} variant="icon" />
```

Для других мест используйте соответствующий вариант:
- Навигация: `variant="icon"` или `variant="compact"`
- Главная страница: `variant="full"`
- Футер: `variant="horizontal"`

